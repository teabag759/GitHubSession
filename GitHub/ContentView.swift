

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, Soy!")
            Text("Hello, Soy!")
            Text("Hello, Soy!")
            Text("Hello, Soy!")
            Text("Hello, Soy!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
